#include "main.h"


/**
 * mul - Multiplies two integers
 * @i: The first number to multiply
 * @j: The second number to multiply with first number
 * Return: The result of multiplication between i param and j param
 */

int mul(int i, int j)

{

	return (i * j);

}
