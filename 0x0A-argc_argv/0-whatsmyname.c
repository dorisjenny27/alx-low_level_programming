#include <stdio.h>
#include "main.h"

/**
* main - this prints the name of the program
* @argc: these are counts of arguments
* @argv: this is an vector of arguments
*
* Return: Always 0 (Success)
*/

int main(int argc, char *argv[])
{
	(void) argc;

printf("%s\n", argv[0]);

return (0);

}
